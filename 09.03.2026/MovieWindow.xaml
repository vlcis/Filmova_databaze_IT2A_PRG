<Window x:Class="_09._03._2026.MovieWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Movie" Height="400" Width="300">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>

            <TextBlock Text="Title"/>
            <TextBox x:Name="TitleBox" Margin="5"/>

            <TextBlock Text="Year"/>
            <TextBox x:Name="YearBox" Margin="5"/>

            <TextBlock Text="Genre"/>
            <TextBox x:Name="GenreBox" Margin="5"/>

            <TextBlock Text="Length"/>
            <TextBox x:Name="LengthBox" Margin="5"/>

            <TextBlock Text="IMDb"/>
            <TextBox x:Name="ImdbBox" Margin="5"/>

            <TextBlock Text="ČSFD"/>
            <TextBox x:Name="CsfdBox" Margin="5"/>

            <TextBlock Text="Rotten"/>
            <TextBox x:Name="RottenBox" Margin="5"/>

        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="5" Click="OK_Click"/>
            <Button Content="Cancel" Width="80" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>