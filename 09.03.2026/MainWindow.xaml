<Window x:Class="_09._03._2026.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Movies" Height="450" Width="800">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <Label Content="Search:"/>
            <TextBox x:Name="SearchBox" Width="200" Margin="5 0"
                     TextChanged="FilterChanged"/>

            <Label Content="Genre:" Margin="20 0 0 0"/>
            <ComboBox x:Name="GenreFilter" Width="150"
                      SelectionChanged="FilterChanged">
                <ComboBoxItem Content="All" IsSelected="True"/>
                <ComboBoxItem Content="Action"/>
                <ComboBoxItem Content="Drama"/>
                <ComboBoxItem Content="Comedy"/>
                <ComboBoxItem Content="Sci-Fi"/>
            </ComboBox>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="MoviesGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Movies}"
                  IsReadOnly="True"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}"/>
                <DataGridTextColumn Header="Year" Binding="{Binding Year}"/>
                <DataGridTextColumn Header="Genre" Binding="{Binding Genre}"/>
                <DataGridTextColumn Header="Length" Binding="{Binding Length}"/>
                <DataGridTextColumn Header="IMDb" Binding="{Binding Imdb}"/>
                <DataGridTextColumn Header="ČSFD" Binding="{Binding Csfd}"/>
                <DataGridTextColumn Header="Rotten" Binding="{Binding Rotten}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add Movie" Width="100" Margin="5" Click="AddMovie_Click"/>
            <Button Content="Edit Movie" Width="100" Margin="5" Click="EditMovie_Click"/>
            <Button Content="Delete Movie" Width="100" Margin="5" Click="DeleteMovie_Click"/>
        </StackPanel>

    </Grid>
</Window>